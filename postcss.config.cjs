// Empty on purpose. Without this, PostCSS walks up and picks up the content portal's
// tailwind config at the repo root, which this site does not use — the styling here is
// hand-written CSS over generated tokens.
module.exports = { plugins: {} };
